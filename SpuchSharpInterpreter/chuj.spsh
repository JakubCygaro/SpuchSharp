import "STDLib";

text[] stack = [5];
int pos = 0;

pub fun print_stack(){
	for x from 0 to pos {
		println(stack[x]);
	}
}

pub fun push(text t){
	stack[pos++] = t;
}
pub fun pop() text {
	return stack[--pos];
}
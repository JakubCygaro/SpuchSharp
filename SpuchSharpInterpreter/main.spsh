import "STDLib";
fun main(){
	[int] array = [10];
	for x from 0 to 9 {
		array[x] = randInt(1, 10);
	}
	println(array);
}
fun printArray([int] array){
	for x from 0 to len(array) - 1 {
		println(array[x]);
	}
}
fun dupa(int x){
	x = x + 10;
}
fun add(int a, int b) int {
	return a + b;
}
=======
	var chuj = {"Adam Papieros", "Jaca", "Kartoniarz" };
	printArray(chuj);
		
}
fun printArray(ref [text] array){
	for x from 0 to len(array) - 1 {
		println(array[x]);
	}
}

